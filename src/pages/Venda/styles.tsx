import styled from "styled-components";

export const ItemVenda = styled.div`
    border-radius: var(--gap);
    background: #fff;
    color: var(--colo-2);
    padding: var(--gap);
    margin-top: var(--gap-s);
`